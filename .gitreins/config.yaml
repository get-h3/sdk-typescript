project: "h3-sdk-typescript"
language: "typescript"

guards:
  secrets:
    enabled: true
    severity: block
  build:
    enabled: false
  lint:
    enabled: false
  tests:
    enabled: false
  dead_code:
    enabled: false

test:
  mode: full

commit:
  require_guard_pass: true
